# Beginner-Programming-with-Functions-Using-built-in-Functions
built-functions

examples of Built-in Functions:
input()
print()


Applications of  Built-in Functions:
# Python modules
import math
import ranodom 

value = math.sqrt(16)
print(Value)
# we would get a value of 4 
# we can use an alias to shorten built-function:
import math as m
value = m.sqrt(16)


